import React from 'react';
import Layout from '../common/Layout';

const Footer = () => {
    return (
        <Layout>
            <footer>푸터입니다</footer>
        </Layout>
    );
};

export default Footer;
